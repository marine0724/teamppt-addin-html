# 실행 프롬프트 — 리디자인 Phase 2 (컨설팅 컨셉: 질문 → 컨셉 3블록 생성·선택)

아래 블록을 **새 세션(opus, 오토모드)**에 그대로 붙여넣어 시작하면, 지금까지와 같은 방식(opus 오케스트레이션 + 서브에이전트 구현)으로 이어집니다. **Phase 2는 아직 플랜이 없으니, 플랜 작성부터 시작합니다.**

---

```
PROGRESS-BOARD.md를 먼저 읽고, 이어서 아래를 읽어줘:
- 스펙: docs/superpowers/specs/2026-06-25-route-b-deck-redesign-design.md (리디자인 전체 설계 — 특히 "Phase 2 — 컨설팅 컨셉(Step 3)" 절과 신규 단위 표)
- Phase 1 결과(완료): 덱 구조 분석 + 파일진입 + 구조박스. 커밋 dd2496a·6053a69·db0a3f2. DeckStructureService/DeckFileReader/AssetPanel 덱 바가 이미 동작(PPT 검증✅).
- 진행 원장: .superpowers/sdd/progress.md (Phase 1까지 complete — git log로 교차확인).

브랜치는 feat/asset-combination-recommendation 그대로. 워크트리 쓰지 마(빌드가 c:\Projects\teamppt-addin 경로에 묶임 — 다른 경로면 PPT 수동검증이 안 맞음).

## 이번 단계 = Phase 2 (컨설팅 컨셉)
스펙 정의: 구조박스 다음에 "어디에 쓰나 / 어떤 느낌" 질문 → 답 + 구조요약 근거로 LLM이 컨셉 3블록 생성(이름 + styleTags + colors/fonts role 맵 = DesignConcept 형태) → 사용자가 카드로 선택. 선택된 컨셉은 D2로 가볍게 두 군데 작용: (a) Phase 3 검색 쿼리에 styleTags 가중, (b) unlocked 색/폰트를 ConceptResolver로 override. 깊은 컨셉 엔진은 후순위.
- 신규 단위: ConceptSuggester(LLM 컨셉 3개 생성, 텍스트 전용 저가 1회), 질문/컨셉 카드 UI(AssetPanel 확장). 기존 DesignConcept/ConceptResolver 재사용.
- 데모 스코프: "진짜(가볍게)". 무겁게 만들지 말 것.

## 먼저 할 일: 플랜 작성 (Phase 2는 플랜 없음)
1. superpowers:brainstorming 으로 Phase 2 범위·UX(질문 몇 개? 어떤 질문? 카드 인터랙션? D2 적용 시점은 Phase 2에서 어디까지?)를 좁혀라. 스펙에 이미 방향이 있으니 빈칸만 메우는 수준.
2. superpowers:writing-plans 로 docs/superpowers/plans/2026-06-26-redesign-phase2-consulting-concept.md 작성. Phase 1 플랜과 같은 형식(Task 단위, 순수로직=TDD / COM·UI=빌드+수동, 파일별 Compile Include 명시, Self-Review).
3. 그 다음에 실행.

## 실행 방식 (Phase 0·1과 동일)
superpowers:subagent-driven-development 로 플랜을 Task 1부터 실행.
- 이 세션(opus)이 오케스트레이션: task-brief 추출 → 구현 서브에이전트 디스패치 → review-package → 리뷰 서브에이전트 → 원장 갱신.
- 모델: 구현 서브에이전트 = sonnet (이 COM/빌드 환경엔 중간티어가 안전). 리뷰 = diff 작으면 haiku, 로직이면 sonnet. 최종 통합 리뷰 = opus.
- 스크립트: C:\Users\michael\.claude\plugins\cache\claude-plugins-official\superpowers\6.0.3\skills\subagent-driven-development\scripts\{task-brief,review-package}

## ⚠ 빌드/테스트 절차 (엄수 — Phase 1에서 확립)
- 단위테스트(순수 로직): MSBuild 솔루션 빌드(RegisterForComInterop=false, 관리자 불필요) → dotnet test --no-build.
  & "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" "c:\Projects\teamppt-addin\src\TeampptAddin\TeampptAddin.sln" /t:Build /p:Configuration=Debug "/p:Platform=Any CPU" /p:RegisterForComInterop=false /verbosity:minimal
  dotnet test "c:\Projects\teamppt-addin\src\TeampptAddin.Tests\TeampptAddin.Tests.csproj" --no-build -p:BuildProjectReferences=false --filter "FullyQualifiedName~<클래스명>"
- dotnet test 단독은 NuGet 못 풀어 실패 → 반드시 MSBuild 먼저.
- 빌드는 foreground (run_in_background 금지). 새 .cs는 메인 csproj <Compile Include> 수동 등록.
- UI/COM 태스크는 단위테스트 대신 빌드 0에러 + 수동검증.

## 불변 원칙
- 사실 = COM / 벡터, 판단 = LLM. 텍스트 내용 생성·수정 금지(컨셉은 역할/스타일만, 텍스트는 COM 원문).
- 디자인-온리(구조·스타일만, 내용 기획 X). 비파괴(원본 초안 불변).
- COM은 STA(UI) 스레드 — 패널 호출 시 ConfigureAwait(false) 금지.
- CoordinateConverter 폴백 로직 추가 금지. 두 토큰 예산(런타임은 싼 호출, 장수에 비례 X).
- API 키 평문 금지. 커밋 메시지 끝에 Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>.

## 진행 규칙
- Task 끝(리뷰 clean)마다 원장에 한 줄 추가, PROGRESS-BOARD 갱신.
- UI 태스크는 마지막에 PPT 수동검증을 사용자에게 넘겨(구조박스 → 질문 → 컨셉 카드 3개 → 선택 흐름이 뜨는지).
- 버그는 추측 말고 superpowers:systematic-debugging.

## Phase 1 미처리(선택, 비차단) — 데모 임박 시 먼저 처리 가능
- ① RunDeckRedesignAsync finally에서 구조박스 없으면 _emptyState 복원.
- ② 실행 중 덱 바 dim(추천 바와 패리티).

플랜 작성부터 시작해줘.
```

---

## 참고 (사람용 메모)

- **Phase 로드맵:** `0 두유사도✅` → `1 파일진입+덱구조✅` → **`2 컨셉3 ◀`** → `3 박스별 덱추천` → `4 빈 템플릿 조립` → `5 재료이식(본문1~2장)`.
- **Phase 1 완료(2026-06-26):** Task 1~3, 최종 리뷰 READY TO MERGE, PPT 구조박스 수동검증 ✅. 데모 hero ① 달성.
- **Phase 2 = 데모 hero 사이 다리:** 구조박스(①) 다음, 박스별 추천+두 유사도(②) 앞. 가볍게 — 컨셉 3개 생성·선택 + D2 두 군데 작용까지만. 깊은 컨셉 엔진은 후순위.
- **재사용:** DesignConcept 모델·ConceptResolver(적용)는 이미 있음 → ConceptSuggester(생성)만 신규 + 카드 UI.
- **세션 절약:** opus 오케스트레이션 + 서브에이전트 구현이면 무거운 구현이 메인 컨텍스트에 안 쌓임. 긴 작업 전 깨끗한 경계에서 /clear 후 이 프롬프트로 재개.
