# Build outputs
src/*/bin/
src/*/obj/

# Visual Studio
.vs/
*.user
*.suo
*.cache

# macOS
.DS_Store

# Thumbnails cache (runtime generated)
thumbnails/

# NuGet
packages/

# API keys / admin credentials (secrets)
**/api-keys.json
**/admin.json

# R&D 에셋 원본 묶음 (공개 repo 노출 금지 — 로컬 보관, 인제스트 테스트용)
assets/layout_test_asset.pptx
assets/header family.pptx

# Release 배포 zip (GitHub Releases에 업로드 — git 히스토리에 바이너리 미적재)
dist/

# Build / verification logs
*.log

# Manual ingest verification output (generated by scripts/manual-verify-task7.ps1)
/test/
.vs\ 
